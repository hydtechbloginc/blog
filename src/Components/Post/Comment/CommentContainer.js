import styled from "styled-components";

export const CommentContainer = styled.div`
  margin-top: 3rem;
  border-radius: 0px;
  padding: 0px 0px;
  position: relative;
  color: ${(props) => props.theme.text};
  border: ${(props) => props.theme.text} solid 0px;
  font-size: 0px;
`;
